public class SecondHighestInteger {
public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int[] array = new int[5];
        int N = sc.nextInt();
        for (int i = 0; i < N; i++) {
            array[i] = sc.nextInt();
        }
        Arrays.sort(array);
        int size = array.length;
        System.out.println("Second Highest = " + array[size - 2]);
    }
